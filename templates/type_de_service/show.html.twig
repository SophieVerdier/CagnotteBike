{% extends 'base.html.twig' %}

{% block title %}TypeDeService{% endblock %}

{% block body %}
    <h1>TypeDeService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_de_service.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_de_service.nom }}</td>
            </tr>
         
        </tbody>
    </table>

    <a href="{{ path('app_type_de_service_index') }}">back to list</a>

    <a href="{{ path('app_type_de_service_edit', {'id': type_de_service.id}) }}">edit</a>

    {{ include('type_de_service/_delete_form.html.twig') }}
{% endblock %}
