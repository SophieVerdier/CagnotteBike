{% extends 'base.html.twig' %}

{% block title %}TypeDeService index{% endblock %}

{% block body %}
    <h1>TypeDeService index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_de_service in type_de_services %}
            <tr>
                <td>{{ type_de_service.id }}</td>
                <td>{{ type_de_service.nom }}</td>
        
                <td>
                    <a href="{{ path('app_type_de_service_show', {'id': type_de_service.id}) }}">show</a>
                    <a href="{{ path('app_type_de_service_edit', {'id': type_de_service.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_de_service_new') }}">Create new</a>
{% endblock %}
